{
  "name": "zdaj-se-pjatk-data-scripts",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "explodeOverrides": "bun run explodeOverrides.ts",
    "explodeOverrides:dev": "bun run --watch explodeOverrides.ts",
    "implodeOverrides": "bun run implodeOverrides.ts",
    "implodeOverrides:dev": "bun run --watch implodeOverrides.ts",
    "validate": "bun run validate.ts",
    "validate:dev": "bun run --watch validate.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ajv": "^8.16.0",
    "chalk": "^5.3.0"
  }
}
